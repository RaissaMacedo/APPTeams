name: CD

on:
  push:
    branches:
      - master

jobs:
  CI:
    name: Continuous Delivery
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3



