"use strict";

/* GENERATED FILE */
import React, { useContext, useMemo } from 'react';
import { IconContext } from '../lib';
import bold from '../bold/MicrosoftExcelLogo';
import duotone from '../duotone/MicrosoftExcelLogo';
import fill from '../fill/MicrosoftExcelLogo';
import light from '../light/MicrosoftExcelLogo';
import regular from '../regular/MicrosoftExcelLogo';
import thin from '../thin/MicrosoftExcelLogo';
import { jsx as _jsx } from "react/jsx-runtime";
function MicrosoftExcelLogo({
  weight,
  color,
  size,
  style,
  mirrored
}) {
  const {
    color: contextColor = '#000',
    size: contextSize = 24,
    weight: contextWeight = 'regular',
    mirrored: contextMirrored = false,
    style: contextStyle
  } = useContext(IconContext);
  const IconComponent = useMemo(() => {
    const iconWeight = weight ?? contextWeight;
    const weightMap = {
      bold,
      duotone,
      fill,
      light,
      regular,
      thin
    };
    return weightMap[iconWeight];
  }, [weight, contextWeight]);
  const mirroredValue = mirrored ?? contextMirrored;
  return /*#__PURE__*/_jsx(IconComponent, {
    color: color ?? contextColor,
    size: size ?? contextSize,
    style: [contextStyle, style, {
      ...(mirroredValue && {
        transform: [{
          scaleX: -1
        }]
      })
    }]
  });
}
export default MicrosoftExcelLogo;
//# sourceMappingURL=MicrosoftExcelLogo.js.map