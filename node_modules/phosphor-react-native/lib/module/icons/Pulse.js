"use strict";

/* GENERATED FILE */
import React, { useContext, useMemo } from 'react';
import { IconContext } from '../lib';
import bold from '../bold/Pulse';
import duotone from '../duotone/Pulse';
import fill from '../fill/Pulse';
import light from '../light/Pulse';
import regular from '../regular/Pulse';
import thin from '../thin/Pulse';
import { jsx as _jsx } from "react/jsx-runtime";
function Pulse({
  weight,
  color,
  size,
  style,
  mirrored
}) {
  const {
    color: contextColor = '#000',
    size: contextSize = 24,
    weight: contextWeight = 'regular',
    mirrored: contextMirrored = false,
    style: contextStyle
  } = useContext(IconContext);
  const IconComponent = useMemo(() => {
    const iconWeight = weight ?? contextWeight;
    const weightMap = {
      bold,
      duotone,
      fill,
      light,
      regular,
      thin
    };
    return weightMap[iconWeight];
  }, [weight, contextWeight]);
  const mirroredValue = mirrored ?? contextMirrored;
  return /*#__PURE__*/_jsx(IconComponent, {
    color: color ?? contextColor,
    size: size ?? contextSize,
    style: [contextStyle, style, {
      ...(mirroredValue && {
        transform: [{
          scaleX: -1
        }]
      })
    }]
  });
}
export default Pulse;
//# sourceMappingURL=Pulse.js.map